{
  "name": "stremio-gdrive-addon-cat",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "lint": "eslint src/index.js",
    "test": "node test/episodis.test.js && node test/multicarpeta.test.js",
    "verify": "npm run lint && npm run test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "eslint": "^9.0.0"
  }
}
